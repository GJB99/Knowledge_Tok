from datetime import datetime
from models import Content
from sqlalchemy.ext.asyncio import AsyncSession

async def seed_initial_content(db: AsyncSession):
    # Empty seed function - no sample data
    pass 